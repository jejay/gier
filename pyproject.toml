[project]
name = "codehierarchy"
version = "0.1.0"
description = "Recognize the code-block structure of a Python file as a single line."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
codehierarchy = "codehierarchy.__main__:main"

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"
